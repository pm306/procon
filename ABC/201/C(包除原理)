別解。難しい。

問題文を言い換える
事象S := 属性がoとsである数字を使った4桁の値の集合

問題の答えは、Sのうち、属性がoである値を全部1回以上使う、という意味になる
例
0123456789
oooo???xxx
のとき
0を1回以上使う and 1を1回以上使う and 2を1回以上使う and 3を1回以上使う
「1回以上」という条件は扱いにくいので、条件の否定を考える
not (0を使わない or 1を使わない or 2を使わない or 3を使わない)
「nを使わない」という条件は、oを×に読み替えている
×なら最初から集合から除外すれば良いので判定が楽になる

notの中身を包除原理で解く
(-1) ** ? * comb() * x**4
oを2個選び×に塗り替えた場合、その選び方は4C2
残った2個は?(使っても使わなくていい)になる
その場合、xは(3 + (4 - 2))種類あり、それを4回選べるので4乗する。

ソースコードでは4個×,3個×,2個×,1個×の順で処理している。
余事象なので正負を反転しておき、最後に全事象を足しているっぽい。
正直よくわからんち。
